# Scenario Spec Reference

Stub. This document will be regenerated from `dwarf/spec/v1/schema.json` once the doc-generation step lands (Slice 1 follow-up).

For now, the authoritative references are:

- `dwarf/spec/v1/schema.json` — machine schema used by the runner.
- `dwarf/spec/v1/schema.yaml` — human-readable mirror with two complete worked examples.
- `dwarf/spec/v1/README.md` — authoring guide.
