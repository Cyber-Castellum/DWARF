"""Static content for /learn/glossary, /learn/faq, /learn/troubleshooting (slice 3)."""
from __future__ import annotations

from typing import Any


GLOSSARY: list[dict[str, str]] = [
    # ──────────────────────────────────────────────────────────────────
    # Dwarf framework terms
    # ──────────────────────────────────────────────────────────────────
    {"term": "Substrate", "definition": "The composed runtime under test — typically a 3-node Cardano network (any mix of cardano-node and Amaru). The substrate is the world the framework observes; everything else (primitives, assertions, faults) acts on it.", "see_also": "/learn/architecture"},
    {"term": "Scenario", "definition": "A YAML file that drives one run. Declares target implementation + version, runtime mode, the substrate composition, the primitive sequence to execute, and the assertions to evaluate. Lives in dwarf/scenarios/.", "see_also": "/operate/scenarios"},
    {"term": "Primitive", "definition": "An atomic action the framework can execute against a substrate — install_version, compose_substrate, byzantine_peer_apply, force_hf_boundary, capture_snapshot, etc. Plugins extend the registry.", "see_also": "/operate/plugins"},
    {"term": "Assertion", "definition": "A post-condition check evaluated against the run's outputs. Common kinds: assert_equal (compare a JSON value to expected), assert_contains, assert_node_consensus, assert_no_panic. Pass/fail rolls up into the bundle's exit_status.", "see_also": "/learn/cli"},
    {"term": "Fault", "definition": "A primitive that DEGRADES the substrate — port_delay, port_drop, byzantine_proxy, snapshot_corrupt. The framework draws the line between observation and degradation so an audit can prove cause/effect.", "see_also": "/learn/examples"},
    {"term": "Forensic bundle", "definition": "A signed, hash-chained tar.gz archive of one run: manifest.json, log.ndjson, outputs/, assertions.json, chain.json. Preserved in dwarf/bundles/. The unit of evidence in CGPL audit packages.", "see_also": "/operate/bundles"},
    {"term": "Manifest hash chain", "definition": "Each bundle's manifest.json includes its hash + the prior bundle's hash, forming an append-only chain. Tampering with any bundle breaks the chain — the inspector's Tamper-check tile validates this on every page load.", "see_also": "/operate/runs"},
    {"term": "Replay determinism", "definition": "A property of a scenario: rerunning it from the same seed against the same target version produces a byte-identical bundle (modulo timestamps). The framework's replay primitive proves this.", "see_also": "/learn/walkthroughs"},
    {"term": "Compose mode", "definition": "How the substrate's nodes are launched. compose_mode=host runs them as native processes via tmux; compose_mode=docker uses docker-compose; compose_mode=multi-host fans out via SSH.", "see_also": "/operate/scenarios"},
    {"term": "Multi-host SSH fan-out", "definition": "compose_mode=multi-host. The framework provisions each node on a different remote box via SSH, observes them through their own SSH channels, and folds the per-host telemetry back into a single bundle.", "see_also": "/learn/walkthroughs"},
    {"term": "AFL++", "definition": "American Fuzzy Lop plus-plus — the fuzzer harness behind Dwarf's parser-fuzz scenarios. The framework wraps cargo-fuzz and AFLNet around AFL++ so the same corpus drives multiple harnesses.", "see_also": "/learn/coverage"},
    {"term": "qemu-mode", "definition": "An AFL++ runtime that instruments stripped binaries (no recompilation needed). Dwarf uses it to fuzz cardano-node release binaries that AFL can't statically instrument.", "see_also": "/learn/coverage"},
    {"term": "AFLNet", "definition": "An AFL++ extension for stateful protocol fuzzing. Dwarf uses it for chainsync / blockfetch / txsubmission grammar-driven harnesses where input is a sequence of mini-protocol messages, not a single byte string.", "see_also": "/learn/coverage"},
    {"term": "SARIF", "definition": "Static Analysis Results Interchange Format — the JSON schema GitHub code-scanning, Sonar, and most audit tools consume. Dwarf's runtime_bundle_export_sarif primitive converts a bundle's findings into SARIF v2.1.0.", "see_also": "/operate/static-analysis"},
    {"term": "Plugin", "definition": "A directory under DWARF_PLUGINS_DIR (default ~/.dwarf/plugins) containing plugin.json + an entrypoint that contributes primitives to the global registry. Audit-grade isolation — plugins can't reach into framework internals.", "see_also": "/operate/plugins"},
    {"term": "Profile", "definition": "A named deployment configuration (host, ssh_user, runtime_root, peer_sharing setting). cardano-profile deploy <profile-id> provisions the substrate. Distinct from a scenario, which is the run plan.", "see_also": "/operate/profiles"},
    {"term": "Tip group count", "definition": "Per-node observation metric — how many distinct chain-tip hashes the node observed during the window. tip_group_count > 1 means the node forked. The honest-baseline scenario asserts == 1 for all nodes.", "see_also": "/operate/runs"},
    {"term": "Byzantine peer", "definition": "A TCP proxy the framework inserts between two substrate nodes that can intercept, mutate, or drop mini-protocol segments. Drives the byzantine fault scenarios.", "see_also": "/learn/examples"},
    {"term": "Era transition", "definition": "A hard-fork boundary (byron→shelley→…→conway). Dwarf's force_hf_boundary primitive fires the boundary at a chosen slot and observation primitives verify the post-rules apply.", "see_also": "/learn/examples"},
    {"term": "Genesis mode", "definition": "Cardano's bootstrap-from-block-zero sync path, distinct from caught-up gossip. Dwarf's genesis_mode_simulate primitive forces a node into genesis mode and watches for peer-set capture attacks.", "see_also": "/learn/examples"},

    # ──────────────────────────────────────────────────────────────────
    # Cardano consensus
    # ──────────────────────────────────────────────────────────────────
    {"term": "Ouroboros", "definition": "The family of provably-secure proof-of-stake consensus protocols Cardano implements (Classic → BFT → Praos → Genesis → currently Praos). Each generation adds capabilities while preserving the chain-quality / chain-growth / common-prefix properties.", "see_also": "https://docs.cardano.org/about-cardano/learn/ouroboros-overview/"},
    {"term": "Praos", "definition": "Ouroboros Praos — the proof-of-stake protocol Cardano runs in production. Slot leaders are selected stake-weighted via VRF; chain quality is preserved as long as <50% of stake is adversarial. Exercised in every honest-baseline scenario.", "see_also": "/learn/examples#runtime-substrate-honest-baseline-example-smoke"},
    {"term": "Slot", "definition": "Cardano's atomic time unit. Mainnet uses 1-second slots; preview/preview2 also 1s but with shorter epochs. Each slot has at most one slot leader; many slots have none. Slot ordering is monotonic — chainsync rejects any header that violates this.", "see_also": "/learn/examples#runtime-substrate-chainsync-nonmonotonic-slot-example-smoke"},
    {"term": "Slot leader", "definition": "The stake-pool the VRF declares eligible to mint a block in a given slot. Other pools may also be eligible (multiple-leader case); ties are resolved by chain weight. Praos's security argument depends on adversaries not knowing leadership in advance.", "see_also": "https://docs.cardano.org/about-cardano/learn/ouroboros-overview/"},
    {"term": "VRF", "definition": "Verifiable Random Function — the cryptographic primitive that picks slot leaders. Each pool has a VRF key; ``VRF(slot, epoch_nonce, vrf_skey)`` returns a value the rest of the network can verify with the public key. Output is uniform-random, leadership-eligible iff the value is below a stake-proportional threshold.", "see_also": "https://eprint.iacr.org/2017/573.pdf"},
    {"term": "KES", "definition": "Key Evolving Signatures. Each pool has a KES key that's used to sign minted blocks. The key evolves forward every N slots and old versions become unusable — protects against retroactive block-forging by a key compromise. Pools must rotate the KES key periodically.", "see_also": "https://docs.cardano.org/about-cardano/learn/cardano-keys/"},
    {"term": "Epoch", "definition": "A fixed-length window of slots. Mainnet: 432,000 slots = 5 days. Stake-snapshot rollovers + reward distribution + protocol-parameter updates fire on epoch boundaries. The k security parameter is measured in slots within an epoch.", "see_also": "/learn/examples#runtime-substrate-stake-snapshot-boundary-example-smoke"},
    {"term": "Epoch boundary", "definition": "The transition between two epochs. Stake snapshots roll forward, rewards are paid out, protocol parameter updates take effect. Substrate scenarios that test epoch-boundary behaviour exercise this transition.", "see_also": "/learn/examples#runtime-substrate-compound-mempool-relay-epoch-boundary-example-smoke"},
    {"term": "k (security parameter)", "definition": "The number of blocks past which a chain switch is forbidden — Cardano's deep-rollback prevention. Mainnet: k=2160. Praos's chain-prefix guarantee says no more than k blocks can be rolled back; nodes refuse to follow a fork that would.", "see_also": "https://eprint.iacr.org/2017/573.pdf"},
    {"term": "Hard fork", "definition": "A protocol-version bump that introduces non-backwards-compatible rules (new ledger features, era transitions, etc.). Cardano's hard-fork combinator orchestrates the switch at a chosen slot; pre/post-HF rules apply on either side. Tested via the era_transition primitive.", "see_also": "/learn/examples#runtime-substrate-era-transition-example-smoke"},

    # ──────────────────────────────────────────────────────────────────
    # Cardano mini-protocols
    # ──────────────────────────────────────────────────────────────────
    {"term": "Mini-protocol", "definition": "Cardano's wire layer is a multiplexed bundle of orthogonal mini-protocols. Each mini-protocol is a small state machine (usually 2-5 states) that handles one job — chain announcement, block transfer, tx gossip, etc. The framework fuzzes each independently AND together.", "see_also": "/learn/coverage"},
    {"term": "Handshake", "definition": "Mini-protocol that negotiates protocol version, network magic, and feature flags between peers. Runs first; if the handshake fails the connection is dropped before any other mini-protocol starts. Validates on-spec version compatibility.", "see_also": "/operate/targets"},
    {"term": "ChainSync", "definition": "Mini-protocol that announces and walks the chain. Server pushes ``MsgRollForward`` (new header) or ``MsgRollBackward`` (rollback to point); client requests ``MsgFindIntersect`` to align. Three reject paths the framework exercises: parent discontinuity, non-monotonic slot, non-incrementing height.", "see_also": "/learn/examples#runtime-substrate-chainsync-parent-discontinuity-example-smoke"},
    {"term": "BlockFetch", "definition": "Mini-protocol for transferring block bodies. Client requests a (from, to) range; server streams blocks with continuity + range-validity checks. Five fault scenarios: invalid range, range pressure, invalid block CBOR, range mismatch, continuity failure.", "see_also": "/learn/examples#runtime-substrate-blockfetch-invalid-range-example-smoke"},
    {"term": "TxSubmission", "definition": "Mini-protocol for tx gossip between nodes. Inflight tx-id window is bounded per-peer; reply must contain only requested bodies. The framework tests window pressure, batch pressure, and unexpected-body cases.", "see_also": "/learn/examples#runtime-substrate-txsubmission-window-pressure-example-smoke"},
    {"term": "KeepAlive", "definition": "Lightweight liveness mini-protocol. Periodically pings the peer; missing replies trigger the disconnect path. Critical during network impairment scenarios — without it slow links would silently drop without recovery.", "see_also": "/learn/examples#runtime-substrate-network-impairment-example-smoke"},
    {"term": "PeerSharing", "definition": "Mini-protocol that lets peers exchange peer-address lists, populating the peer governor's cold pool. Eclipse attacks attempt to capture this exchange to feed only adversarial addresses. Disabled by default in some profiles for security-sensitive deployments.", "see_also": "/operate/profiles"},
    {"term": "LocalStateQuery", "definition": "Local-only mini-protocol — a node's own client (cardano-cli, cardano-db-sync, etc.) queries ledger state. Stateful: client establishes a query session, asks queries, releases. Used by wallets, indexers, and the Dwarf inspect path.", "see_also": "/learn/cli"},
    {"term": "LocalTxSubmission", "definition": "Local-only mini-protocol for submitting transactions from a wallet to the node. Client submits a Tx; node returns SubmitFail or SubmitSuccess. Distinct from peer TxSubmission (which gossips between nodes).", "see_also": "/operate/targets"},
    {"term": "LocalTxMonitor", "definition": "Local-only mini-protocol for inspecting the node's mempool. Client subscribes; server pushes mempool snapshots and notifications when txs enter/leave. Used by db-sync and similar indexers.", "see_also": "/operate/targets"},

    # ──────────────────────────────────────────────────────────────────
    # Cardano eras
    # ──────────────────────────────────────────────────────────────────
    {"term": "Byron era", "definition": "Cardano's bootstrap era (Sep 2017 – Jul 2020). Federated genesis block production by IOG/Emurgo/Cardano-Foundation. Pre-Praos, pre-stake-pool. Original Daedalus wallet ran here. Replaced by Shelley.", "see_also": "https://docs.cardano.org/about-cardano/evolution/byron/"},
    {"term": "Shelley era", "definition": "Decentralisation era (Jul 2020). Praos consensus + stake pools + delegation + KES + VRF. The era where Cardano became a permissionless PoS network. Foundation everything else builds on.", "see_also": "https://docs.cardano.org/about-cardano/evolution/shelley/"},
    {"term": "Allegra era", "definition": "Token-locking era (Dec 2020). Added time-lock scripts (validity intervals on transactions). Small step toward Goguen but still no smart contracts. Existed mostly to prove the era-transition machinery before Mary.", "see_also": "https://docs.cardano.org/about-cardano/evolution/allegra/"},
    {"term": "Mary era", "definition": "Multi-asset era (Mar 2021). Native tokens — non-ada assets transactable with the same UTxO machinery as ada. No new validation logic; just an extension to Value. Birth of Cardano NFTs.", "see_also": "https://docs.cardano.org/about-cardano/evolution/mary/"},
    {"term": "Alonzo era", "definition": "Smart-contracts era (Sep 2021). Plutus V1 — first execution layer for on-chain scripts. Introduced collateral inputs, script-context, ex-units accounting. The era that made Cardano programmable.", "see_also": "https://docs.cardano.org/about-cardano/evolution/alonzo/"},
    {"term": "Babbage era", "definition": "Plutus V2 era (Sep 2022, Vasil HF). Reference inputs, inline datums, reference scripts, more efficient script execution. Reduced transaction sizes substantially. The era running on mainnet immediately before Conway.", "see_also": "https://docs.cardano.org/about-cardano/evolution/babbage/"},
    {"term": "Conway era", "definition": "Governance era (Sep 2024, Chang HF). On-chain CIP-1694 governance — voting roles (DReps, SPOs, CC), governance actions, treasury withdrawals via vote. Plutus V3 ships here. Current mainnet era.", "see_also": "https://docs.cardano.org/about-cardano/evolution/conway/"},

    # ──────────────────────────────────────────────────────────────────
    # Topology / peer governance
    # ──────────────────────────────────────────────────────────────────
    {"term": "Peer governor", "definition": "The component inside cardano-node that decides which peers to keep connected to. Maintains hot / warm / cold pools and rotates between them based on liveness, latency, and chain freshness signals. Eclipse-attack scenarios target this.", "see_also": "/learn/examples#runtime-substrate-compound-eclipse-recovery-example-smoke"},
    {"term": "Hot peer", "definition": "A peer the governor is actively running every mini-protocol against. Hot peers carry chain-relay traffic. The governor maintains a target hot-peer count (typically 3-5).", "see_also": "/operate/profiles"},
    {"term": "Warm peer", "definition": "A peer the governor has handshaked with but isn't actively running ChainSync against. Held in reserve for promotion to hot when an existing hot peer drops. Bounded count.", "see_also": "/operate/profiles"},
    {"term": "Cold peer", "definition": "A peer-address the governor knows but hasn't connected to recently. Cold pool feeds promotions to warm; sourced from PeerSharing exchanges, ledger peers, and configured local roots.", "see_also": "/operate/profiles"},
    {"term": "Big-ledger peer", "definition": "A peer associated with a stake pool that has substantial active stake. Cardano consensus prefers connecting to big-ledger peers because they're statistically less likely to be Sybils — adversary needs real ada to forge them.", "see_also": "https://github.com/IntersectMBO/ouroboros-consensus/blob/main/docs/website/contents/for-developers/Peers.md"},
    {"term": "Ledger peer", "definition": "A peer drawn from on-chain stake-pool registrations. Distinct from local-roots (operator-configured) and shared peers (PeerSharing-discovered). Ledger peers give Sybil-resistance because forging requires ada.", "see_also": "/operate/profiles"},
    {"term": "Trustable peer", "definition": "A peer the operator has manually marked as trusted via the topology config (typically the operator's own block-producer or relay). Bypasses some governor heuristics — used carefully.", "see_also": "/operate/profiles"},

    # ──────────────────────────────────────────────────────────────────
    # Network identifiers
    # ──────────────────────────────────────────────────────────────────
    {"term": "Network magic", "definition": "A 32-bit integer that distinguishes one Cardano network from another at handshake time. Mainnet = 764824073, testnet (legacy) = 1097911063, preview = 2, preview2 = 4. Misconfigured magic = handshake failure.", "see_also": "/learn/glossary#term-handshake"},
    {"term": "Mainnet", "definition": "Cardano's production network. Network magic 764824073. Real ada, real stake pools, mainline release timeline. The framework's substrate scenarios target preview/preview2 by default — never mainnet.", "see_also": "https://docs.cardano.org/about-cardano/explore-more/cardano-networks/"},
    {"term": "Preview", "definition": "Long-running test network for IOG-hosted release candidates. Network magic 2. Stable, fewer reset events. The framework targets this for substrate compose by default.", "see_also": "/operate/scenarios"},
    {"term": "Preview2", "definition": "The successor to preview, post-Conway-HF reset. Network magic 4. Sometimes called preprod-preview or pv2 in scripts.", "see_also": "/operate/profiles"},
    {"term": "Testnet 42", "definition": "Local devnet used in Dwarf's host-mode and docker-mode scenarios. Network magic 42 (deliberately distinct from any public network). Genesis is generated by the framework, so substrate runs are reproducible bit-for-bit.", "see_also": "/learn/examples#runtime-substrate-honest-baseline-example-smoke"},
]


FAQ: list[dict[str, str]] = [
    {"q": "How do I run my first scenario?", "a": "Open <a href=\"/learn/getting-started\">/learn/getting-started</a> — a 5-step walkthrough. The TL;DR is <code>cardano-profile scenario run dwarf/scenarios/runtime-substrate-honest-baseline-example-smoke.yaml</code>."},
    {"q": "Where do bundles live?", "a": "Two locations. dwarf/runs/&lt;run-id&gt;/ holds every run the framework has produced. dwarf/bundles/&lt;run-id&gt;.tar.gz holds explicitly-preserved bundles operators have promoted via cardano-profile bundle &lt;run-id&gt; or the Export bundle button in the inspector."},
    {"q": "What's the difference between compose_mode docker and host?", "a": "compose_mode=host runs each substrate node as a native process via tmux on the local machine. compose_mode=docker uses docker-compose to spin them up as containers. compose_mode=multi-host fans out via SSH to multiple remote boxes. The substrate primitive is mode-agnostic — observation tiles look identical across modes — but performance, isolation, and reachability differ."},
    {"q": "How do I add my own primitive?", "a": "Drop a plugin directory under ~/.dwarf/plugins/&lt;your-plugin&gt;/ with a plugin.json (schema in <a href=\"/operate/plugins\">/operate/plugins</a>) and an entrypoint that calls registry.register(). Restart the dashboard so the loader picks it up."},
    {"q": "How do I deploy on another box?", "a": "Copy dwarf/state/config.yaml on the source box, edit host / ssh_user / ssh_key_path / remote_base_path, then run cardano-profile prereq-check followed by cardano-profile deploy &lt;profile-id&gt;. The Profiles page (<a href=\"/operate/profiles\">/operate/profiles</a>) shows every catalogued deployment configuration."},
    {"q": "What's the difference between Phase 4 and qemu-mode?", "a": "Phase 4 is Dwarf's runtime fault-injection layer (port_delay, port_drop, byzantine_proxy) operating on a live substrate. qemu-mode is an AFL++ feature for fuzzing stripped binaries via QEMU instrumentation. Different layers — Phase 4 perturbs the running protocol; qemu-mode mutates binary inputs."},
    {"q": "Why is /operate/runs/<id> showing 'Run not found'?", "a": "The run-id directory isn't on disk. Check dwarf/runs/&lt;id&gt;/ exists, that ADA2_DWARF_RUNS_DIR points to the right place, and that the bundle hasn't been moved into bundles/. Cleanup scripts can prune old runs — the bundle archive in bundles/ is still there, but the unpacked tree may have been trimmed."},
    {"q": "How do I import a bundle from another machine?", "a": "Use the Import form on <a href=\"/operate/bundles\">/operate/bundles</a> (drag-drop a tar.gz) or the CLI: cardano-profile bundle import &lt;path&gt;. The framework verifies the manifest hash chain before unpacking. Existing run-ids are not overwritten."},
    {"q": "How do I switch the dashboard theme?", "a": "Use the Theme picker in the page header (top-right). Choice persists for 365 days via the dwarf_theme cookie. See <a href=\"/static/THEME.md\">THEME.md</a> for the contract and how to add a custom theme."},
    {"q": "Where does config get loaded from?", "a": "Precedence: env var ADA2_PROFILE_MANAGER_CONFIG &gt; dwarf/state/config.yaml &gt; built-in defaults. View the resolved table at <a href=\"/operate/config\">/operate/config</a>. Edit via cardano-profile config set &lt;key&gt; &lt;value&gt; — the dashboard never mutates config."},
    {"q": "How do I wire up Slack notifications?", "a": "Drop a notifications: section into dwarf/state/config.yaml with handlers per event type (on_scenario_fail / on_coverage_regression / on_assertion_population_shift). Each handler is type: slack with url: &lt;webhook-url&gt;. View the wired handlers at <a href=\"/operate/notifications\">/operate/notifications</a>; sample payloads at /static/THEME.md and the docs/notifications.md primer."},
    {"q": "What are the substrate-evidence tiles?", "a": "Six rolled-up summaries the inspector shows above the assertions table when a run produces the matching outputs: Topology (compose-report), Multi-node observation (tip_group_count per node), Byzantine peer (intercepted/mutated counts), HF boundary (per-node protocol versions), Era transition (pre/post rules observed), Genesis mode (final mode + peer-set capture). Each tile turns crimson when its verdict flips error."},
    {"q": "Can I scrape the dashboard?", "a": "Use /metrics for Prometheus, /healthz for liveness, /api/* for JSON. /operate/* and /learn/* are HTML and not stable contracts — see <a href=\"/learn/api\">/learn/api</a> for the machine-readable surface."},
    {"q": "What does the breathing background mean?", "a": "Pure decoration — drift gradient + sparse cyan motes. Reads as 'the substrate is alive.' prefers-reduced-motion freezes it. No state coupling — motion doesn't change with substrate health."},
    {"q": "How do I export a bundle for an audit package?", "a": "From the inspector header, click 'Export bundle (.tar.gz)' — the framework streams the verified bundle archive directly. CLI equivalent: cardano-profile bundle &lt;run-id&gt; (preserves into bundles/ directory). For SARIF export: cardano-profile scenario run dwarf/scenarios/runtime-bundle-export-sarif-example-smoke.yaml after editing target_run_id."},
]


TROUBLESHOOTING: list[dict[str, str]] = [
    {"symptom": "rsync reports 'sent N bytes' but the destination md5 is unchanged",
     "diagnosis": "Common when rsync's --delete + nested-archive optimisations decide a delta-transfer succeeded but the destination file remained the prior version. Hits when deploying to a host that's also being touched by a separate rebuild pipeline.",
     "fix": "Switch to <code>scp</code> for code transfers (it always overwrites). Or pass <code>--inplace --no-whole-file</code> to force rsync to actually rewrite. Verify post-transfer with <code>md5sum</code>."},
    {"symptom": "Dashboard returns 200 on the static asset URL but the page renders unstyled",
     "diagnosis": "DASHBOARD_ROOT path resolution picked the wrong candidate. Common when /home/&lt;user&gt;/dwarf/dashboard/ exists as a stale leftover (only an index.html, no static/) — the picker prefers the candidate whose static/ subdir actually exists, but a totally-empty leftover defeats both code paths.",
     "fix": "<code>rm -rf /home/&lt;user&gt;/dwarf</code> if it's stale, then <code>mkdir /home/&lt;user&gt;/dwarf-fw/agent</code> so _discover_project_root pins the right candidate. Verify with <code>python3 -c \"from profile_manager.dashboard import DASHBOARD_ROOT; print(DASHBOARD_ROOT)\"</code>."},
    {"symptom": "<code>pgrep -f 'dashboard serve' | head -1</code> returns a PID but kill -9 of that PID doesn't free port 8787",
     "diagnosis": "The framework's <code>./cardano-profile dashboard serve</code> spawns a python child that owns the listening socket; the parent shell wrapper has a different PID. <code>head -1</code> returns the first match in PID order, which is sometimes the wrapper, not the listener.",
     "fix": "Target the listener directly: <code>PID=$(ss -tlnp 2>/dev/null | grep 8787 | grep -oE 'pid=[0-9]+' | head -1 | cut -d= -f2); kill -9 $PID</code>. The ss approach reads the socket→pid mapping from the kernel and is unambiguous."},
    {"symptom": "Code on /home/&lt;user&gt;/dwarf-fw is updated but the running dashboard serves the old version",
     "diagnosis": "The dashboard is running inside the V3 Docker container (dwarf-fw-june-m2, image dwarf/framework:june-20260604-m2). The framework source inside the image is baked at build time; host-side runtime directories are mounted only for runs, state, and bundles.",
     "fix": "<code>delivery/scripts/build-image.sh && delivery/scripts/deploy.sh</code>. Verify with <code>docker exec dwarf-fw-june-m2 python3 dwarf/cardano-profile dashboard status</code>."},
    {"symptom": "ImportError: cannot import name 'X' from 'profile_manager.Y'",
     "diagnosis": "The container's profile_manager predates the import the dashboard now needs (e.g. plugin_loader added after the image was built; CONFIG_FIELDS added after, etc.). The image-rebuild pipeline lags head-of-tree.",
     "fix": "Either docker cp the missing module into the container (short-term), or add a graceful fallback in the importer (preferred for code that has to ship before the next image build). Pattern: <code>try: from profile_manager.X import Y; except ImportError: Y = &lt;fallback&gt;</code>."},
    {"symptom": "tmux session fails to attach with '/tmp/tmux-1000/default already exists'",
     "diagnosis": "Stale tmux socket from a prior run. Common after an unclean exit (kill -9, container restart, host reboot).",
     "fix": "<code>rm /tmp/tmux-$(id -u)/default</code> and retry. Or kill any orphan tmux server first: <code>tmux kill-server</code>."},
    {"symptom": "Cardano-node socket path 108-byte limit exceeded",
     "diagnosis": "Unix domain socket paths max out at 108 bytes (107 chars + null). cardano-node creates its socket under the runtime_root path, so deeply-nested compose layouts can exceed the limit. Symptom: silent connection refused on ChainSync.",
     "fix": "Shorten the runtime_root path (move it closer to /). Set remote_base_path to something like /opt/dwarf/&lt;profile&gt;/ rather than /home/&lt;user&gt;/cardano-profiles/&lt;profile&gt;/. The framework's cardano-profile prereq-check warns when the resolved socket path approaches the limit."},
    {"symptom": "dashboard's substrate-compose tile reads 'unknown' mode",
     "diagnosis": "The compose-report.json doesn't carry a 'mode' field. Older substrate-compose primitives (pre-slice 41) didn't emit it; the dashboard tile defaults to 'unknown' rather than failing.",
     "fix": "Re-run with a current scenario; the canonical templates emit mode. Existing bundles can be patched manually if needed (edit outputs/substrate-compose/compose-report.json, set 'mode': 'host'|'docker'|'multi-host')."},
    {"symptom": "/operate/runs/&lt;id&gt; shows 'Run not found' for a real run-id",
     "diagnosis": "The run directory isn't where the dashboard expects. Check ADA2_DWARF_RUNS_DIR env var, /var/dwarf/runs (container), or the host-side dwarf/runs/. Also: the container has a runs-dir mount at /var/dwarf/runs; if that mount missed, the dashboard falls back to its own internal path.",
     "fix": "<code>docker inspect dwarf-fw-june-m2 --format '{{ json .Mounts }}'</code> to verify the runs-dir mount is wired. If it's missing, redeploy with the delivery scripts."},
    {"symptom": "AFL++ fuzzer crashes on cargo-fuzz target with 'Inconsistent timing' warning",
     "diagnosis": "cgroup CPU-quota throttling. Containers with cpu-shares set will trip AFL's timing-consistency check.",
     "fix": "Drop the cpu cap on the fuzzer container, or pass <code>AFL_NO_AFFINITY=1</code> + <code>AFL_SKIP_CPUFREQ=1</code> in the fuzzer env. Trade-off: the latter ignores the timing variance and proceeds anyway."},
    {"symptom": "Caddy returns 502 on /operate/runs/&lt;id&gt;/tail (SSE) after ~30s",
     "diagnosis": "Caddy's default proxy timeout closes long-running streams. The /tail endpoint is intentionally long-lived (up to 600s idle).",
     "fix": "Add <code>flush_interval -1</code> + <code>transport http { read_timeout 0 }</code> to the Caddyfile reverse_proxy block. This lets Caddy stream SSE chunks immediately without buffering and removes the upstream-read timeout."},
    {"symptom": "Bundle import upload fails with 'helper script not found'",
     "diagnosis": "The container's dwarf/scripts/bundle_import.py is missing — the baked image predates the import primitive.",
     "fix": "Rebuild and redeploy the V3 image from the delivery package. Or use the CLI directly from a host with the script: <code>cardano-profile bundle import &lt;path&gt;</code>."},
]


def docs_payload() -> dict[str, Any]:
    return {"glossary": GLOSSARY, "faq": FAQ, "troubleshooting": TROUBLESHOOTING}
