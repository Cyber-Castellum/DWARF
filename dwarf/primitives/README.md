# Dwarf V3 Milestone 2 Primitive Catalog

This delivery includes the `registry.json` and primitive schemas needed by the selected Milestone 2 (M2) scenarios.

The framework implementation code still lives in `dwarf/profile_manager/`, and the shipped registry advertises the M2 subset used by this package.
